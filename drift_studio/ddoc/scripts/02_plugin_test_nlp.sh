# DEPRECATED — `ddoc transform` was a v1 command and is removed in v2.
# Equivalent v2 functionality lives inside plugins (e.g.
# ddoc-plugin-text's eda_run / drift_detect hookimpls operate directly
# on data paths). Kept as a stub for migration searchers.
echo "v1 'ddoc transform' is removed in v2. See: ddoc analyze eda --help"
